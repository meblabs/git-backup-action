# Copy to .env and fill in your values, then run ./setup.sh
# .env is git-ignored — never commit real values.

AWS_PROFILE=default
AWS_DEFAULT_REGION=eu-west-1
ACCOUNT_ID=123456789012

BUCKET=
ORG=
BACKUP_REPO="git-backup"
